#!/usr/bin/env ruby

require 'erb'

# puts ENV["CONNECTION_URL"]
template = File.read '/workspaces/aws-bootcamp-cruddur-2023/bin/erb/backend-flask.env.erb'
content = ERB.new(template).result(binding)
filename = "/workspaces/aws-bootcamp-cruddur-2023/backend-flask.env"
File.write(filename, content)